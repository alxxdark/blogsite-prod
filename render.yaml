services:
  - type: web
    name: blogsite-prod
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn blog_main.wsgi --log-file -
